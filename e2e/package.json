{
  "name": "brainbytes-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for BrainBytes application",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand --detectOpenHandles --forceExit --verbose",
    "test:headless": "jest --runInBand --detectOpenHandles --forceExit --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "axios": "^1.6.0"
  }
}