{
    "name": "brainbytes-api",
    "version": "1.0.0",
    "description": "Backend API for BrainBytes",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "test": "NODE_ENV=test jest",
        "test:watch": "NODE_ENV=test jest --watch",
        "test:coverage": "NODE_ENV=test jest --coverage",
        "test:api": "NODE_ENV=test jest tests/api.test.js",
        "test:db": "NODE_ENV=test jest tests/db-persistence.test.js"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.17.1",
        "mongoose": "^8.14.2",
        "node-fetch": "^2.6.7"
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "supertest": "^7.1.1",
        "nodemon": "^3.0.0"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}